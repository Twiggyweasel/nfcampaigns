<div class='row'>
  <div class='col-md-6 offset-md-3'>
    <div class='alert alert-danger'>
      <strong><%= pluralize(errors.count, "error") %> prohibited this event from being saved:</strong>
      <ul>
        <% errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>