<%= render 'shared/title', title: "Registration for #{@event.name}", display: 4 %>


<% if @attendee.category == 'Personal' || @attendee.category == "Personal Fundraising" %>

<%= render 'personal' %>

<% else %>

<%= render 'corporate' %>

<% end %>

