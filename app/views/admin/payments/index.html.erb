<%= render 'shared/title', title: 'Payment Attempts', display: 4 %>
<!--<div class="well">-->
<!--<#%= link_to "New Payment", new_payment_path, class: "btn btn-primary" %>-->
<!--</div>-->

<div class='row'>
  <div class='col-12'>
    <table class="table table-bordered table-striped">
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Last 4</th>
        <th>Amount</th>
        <th>Approved</th>
        <th>Authorization Code</th>
        <th>Payable Reference</th>
        <th>Transaction Date</th>
      </tr>
      <% if @payments.size > 0 %>
        <% @payments.each do |payment| %>
          <tr>
            <td><%= payment.first_name %> <%= payment.payable_id %></td>
            <td><%= payment.last_name %></td>
            <td><%= payment.last4 %></td>
            <td><%= number_to_currency payment.amount %></td>
            <td><%= payment.success == true ? "Yes" : "No" %></td>
            <td><%= payment.authorization_code %></td>
            <td><% if payment.payable_type == "Attendee" %> <%= link_to payment.payable_type, admin_event_attendee_path(payment.payable.event, payment.payable_id) %> <% else %> <%= payment.payable_type %> <% end %></td>
            <td><%= payment.created_at.strftime("%m/%d/%Y") %></td>
          </tr>
        <% end %>
      <% else %>
        <tr>
          <td colspan="5">No payments have been attempted.</td>
        </tr>
      <% end %>
    </table>    
  </div>
</div>

